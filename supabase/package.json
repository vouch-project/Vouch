{
  "name": "supabase",
  "version": "1.0.0",
  "description": "Seed scripts for Supabase database.",
  "main": "seed.ts",
  "scripts": {
    "setup": "supabase db reset && tsc seed.ts --outDir dist && node dist/seed.js"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/pg": "^8.20.0",
    "typescript": "^5.3.3"
  }
}
